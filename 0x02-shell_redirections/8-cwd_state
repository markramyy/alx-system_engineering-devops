#!/bin/bash

# Check if the file exists
if [ -f ls_cwd_content ]; then
    rm ls_cwd_content
fi

# Execute the command and write output to file
ls -la > ls_cwd_content

echo "ls -la output written to ls_cwd_content file."
